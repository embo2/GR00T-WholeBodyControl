[project]
name = "brainco"
version = "0.1.0"
description = "Sandbox for talking to BrainCo RevoHand via the official Python SDK."
requires-python = ">=3.10,<3.13"
dependencies = [
    "msgpack",
    "zmq",
]

[tool.uv]
package = false
